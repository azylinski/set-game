export const FEATURES: Array<string> = ["Number", "Symbol", "Shading", "Color"];

export enum Nmbr {
  One = "one",
  Two = "two",
  Three = "three",
}

export enum Smbl {
  Diamond = "diamond",
  Squiggle = "squiggle",
  Oval = "oval",
}

export enum Shading {
  Solid = "solid",
  Striped = "striped",
  Open = "open",
}

export enum Color {
  Red = "red",
  Green = "green",
  Purple = "purple",
}
